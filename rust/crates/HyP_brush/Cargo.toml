[package]
name = "hyp_brush"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
hyp_core = { path = "../HyP_core" }
hyp_tiles = { path = "../HyP_tiles" }

[lib]
path = "src/lib.rs"
